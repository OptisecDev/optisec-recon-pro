# Web framework
fastapi==0.138.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32
jinja2==3.1.6
aiofiles==25.1.0

# HTTP & parsing
httpx==0.28.1
requests==2.34.2
beautifulsoup4==4.15.0

# Data validation
pydantic==2.13.4

# Database — SQLAlchemy async + drivers
sqlalchemy==2.0.51
asyncpg==0.31.0
aiosqlite==0.22.1
alembic==1.18.5
psycopg2-binary==2.9.12

# Eternal Core — hybrid TimescaleDB + MinIO storage
pandas==3.0.5
pyarrow==25.0.1
minio==7.2.20
Pillow==12.2.0

# Auth — JWT + password hashing
python-jose[cryptography]==3.5.0
bcrypt==4.0.1

# Background tasks & WebSocket
apscheduler==3.11.2
websockets==16.0

# Security scanning
dnspython==2.8.0
python-whois==0.9.6

# Reports
reportlab==4.5.1

# AI
groq==1.5.0

# CLI
colorama==0.4.6
rich==15.0.0
click==8.4.2

# Config
python-dotenv==1.2.2

# OSINT Engine
phonenumbers==9.0.33
aiohttp==3.14.1
ua-parser==1.0.2
requests-cache==1.3.2

# Unified OSINT Engine v5.0 — Python-installable tool wrappers
# NOTE: Amass is a Go binary — installed at ~/bin/amass v5.1.1
#   Source: https://github.com/owasp-amass/amass/releases/download/v5.1.1/amass_linux_amd64.tar.gz
#   Ensure ~/bin is in PATH: export PATH="$HOME/bin:$PATH"
# NOTE: theHarvester is not on PyPI (the pypi package is a stub) — install via:
#   pip install git+https://github.com/laramies/theHarvester.git@4.11.1
# Pinned to tag 4.11.1 (requires-python >=3.12) — theHarvester's unreleased
# dev branch bumped to requires-python >=3.14, which broke installs under
# this project's python:3.12-slim base image when this was an unpinned ref.
git+https://github.com/laramies/theHarvester.git@4.11.1
maigret==0.6.1
holehe==1.61

# Cryptography (Quantum-Safe Encryption AES-GCM)
cryptography==49.0.0

# QR Code generation (WireGuard peer configs)
qrcode[pil]==8.2

# Optional: Real PQC (requires liboqs system library)
# liboqs-python>=0.10.0
